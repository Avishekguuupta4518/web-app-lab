// Create a React application that uses state to maintain a counter value. Provide buttons to increment, decrement, and reset the counter. Display the updated counter value dynamically on the screen.

import React from "react";
import Counter from "./component/Counter";

function App() {
  return <Counter />;
}

export default App;